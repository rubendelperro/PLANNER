import '@percy/cypress';
import 'cypress-mochawesome-reporter/register';

// Visual testing: Percy is registered above; call cy.percySnapshot('name') in specs.

// Optional: helpers for tests can go here
